# Label

A single line of text.

```lua
section:Label({
    Text = "Plain label",
    ShowContainer = false,
    ShowStroke = false,
    FontWeight = "Bold",
})
```

## Options

| Key | Type | Description |
| --- | --- | --- |
| `Text` | string | The text. |
| `ShowContainer` | bool | Show the card background. |
| `ShowStroke` | bool | Show the border. |
| `FontWeight` | string | `Regular`, `Medium`, `SemiBold`, `Bold`, `Heavy`, `ExtraBold`. |
